# 계층형 이슈 그리드 — 화면·API 설계 문서

| 항목        | 내용                                                                                                    |
| ----------- | ------------------------------------------------------------------------------------------------------- |
| 버전        | v1.0 (2026-08-18)                                                                                       |
| 대상 독자   | 구현 담당 AI 및 개발자                                                                                  |
| 범위        | 이슈/하위 이슈 계층 조회 화면 1개와 해당 조회 API. 생성·수정·상세 화면은 범위 외                        |
| 프론트 스택 | React + TypeScript + Vite, TanStack Query(react-query) v5, AG Grid v33 이상 (2026-08 기준 최신 v36.0.x) |
| 백엔드 전제 | Spring Boot + Oracle 기준으로 서술. 다른 스택이면 동등 개념으로 치환 가능                               |
| 환경        | 폐쇄망. 패키지는 내부 Nexus 경유                                                                        |

## 0. 문서 사용법

이 문서는 구현 지침이다. "설계 결정(D1~D8)"은 합의된 사항이므로 임의 변경하지 않는다. 그 외 "지침" 항목은 동등한 결과를 내는 한 구현 재량을 허용한다. 코드 예시는 의도적으로 배제했으며, 계약(파라미터·필드·동작 규칙)은 표와 텍스트로 정의한다.

## 1. 요구사항 요약

1. 이슈(상위)와 하위 이슈를 한 그리드에 함께 표시한다.
2. 하위 이슈는 항상 자신의 상위 이슈 아래에 그룹되어 표시된다.
3. 페이징을 지원한다.
4. 검색(키워드) 및 필터(상태, 담당자 등)를 지원한다.

## 2. 용어 정의

| 용어                           | 정의                                                                                   |
| ------------------------------ | -------------------------------------------------------------------------------------- |
| 상위 이슈 (parent)             | `parent_id`가 없는 최상위 이슈                                                         |
| 하위 이슈 (child)              | 특정 상위 이슈에 속한 이슈. 계층은 2단계로 고정 (하위의 하위 없음)                     |
| 직접 매칭 (matched)            | 해당 행 자체가 현재 검색/필터 조건을 만족함                                            |
| 컨텍스트 상위 (context parent) | 본인은 조건에 안 걸리지만, 하위가 매칭되어 문맥 제공을 위해 결과에 포함된 상위 이슈    |
| 그룹 무결성                    | 결과에 포함된 상위 이슈의 하위들이 페이지 경계나 필터에 의해 상위와 분리되지 않는 성질 |

## 3. 선행 확인 사항 (P0): AG Grid 라이선스

AG Grid의 **Tree Data와 Row Grouping은 Enterprise 전용 기능**이다 (공식 문서 명시, 라이선스 키 필요). 구현 착수 전에 조직의 AG Grid Enterprise 라이선스 보유 여부를 반드시 확인하고, 결과에 따라 렌더링 전략을 택한다.

- **경로 A (Enterprise 보유)**: Tree Data + `treeDataChildrenField` 사용. 계층 렌더링·펼침/접힘·들여쓰기를 그리드가 제공.
- **경로 B (Community만 사용)**: 서버 정렬을 전제로 한 flatten 커스텀 렌더링. 계층 표현을 커스텀 셀 렌더러로 직접 구현.

API 설계와 서버 구현(6~7장)은 두 경로에서 완전히 동일하다. 갈라지는 것은 프론트 렌더링(9.4 vs 9.5)뿐이다.

## 4. 핵심 설계 결정

### D1. 페이징 단위 = 상위 이슈

- **결정**: `size=20`은 "상위 이슈 20건 + 그에 속한 하위 전부"를 의미한다. 페이지당 실제 행 수는 가변이다.
- **근거**: 행 단위(flatten) 페이징은 하위가 페이지 경계에서 잘려 "부모 없는 하위가 페이지 상단에 뜨는" 상황을 만들며, 그룹핑 요건과 정면 충돌한다.
- **영향**: count·페이저·"N건" 표기가 모두 상위 이슈 기준이 된다. UI에 단위를 명시해야 한다 (8.1).

### D2. 필터 포함 규칙 = 본인 OR 하위 매칭

- **결정**: 상위 이슈는 (본인이 조건을 만족) OR (하위 중 하나라도 조건을 만족)일 때 결과에 포함된다.
- **근거**: 검색어가 하위에만 걸릴 때 하위를 최상위로 승격시키는 방식(Linear 스타일)은 그룹핑 요건과 충돌한다. 상위를 컨텍스트로 함께 보여주는 것이 요건에 부합한다.
- **영향**: 서버 쿼리에 EXISTS 서브쿼리가 필요하다 (7.1). 컨텍스트 상위의 시각 구분이 필요하다 (D4, 8.3).

### D3. 하위 반환 범위 = 포함된 상위의 하위 전체 (MVP)

- **결정**: 결과에 포함된 상위 이슈에 대해서는, 필터 매칭 여부와 무관하게 **하위 전체**를 반환한다.
- **근거**: 2단계 계층에 fan-out이 크지 않은 내부 툴 규모에서는 부분 반환+lazy load의 복잡도가 이득보다 크다. "매칭된 하위만 + 나머지 lazy load"는 이후 필요 시 확장한다 (13장).
- **영향**: 매칭 안 된 하위도 그리드에 표시되므로, matched 구분이 하위에도 필요하다 (D4).

### D4. matched 플래그로 직접/컨텍스트 구분

- **결정**: 응답의 모든 행(상위·하위)에 boolean `matched`를 부여한다. 매칭성 필터(q, status, assigneeId 등)가 하나도 없으면 전 행 `true`.
- **근거**: "검색 결과로서 걸린 행"과 "문맥 제공을 위해 딸려온 행"을 UI가 구분할 수 있어야 사용자가 혼란스럽지 않다.
- **영향**: 컨텍스트 상위는 dimmed + "하위 매칭" 뱃지, 매칭 안 된 하위는 dimmed 처리 (8.3).

### D5. 정렬 = 서버 단일 책임, 층위별 적용

- **결정**: 정렬은 서버에서만 수행한다. 정렬 기준은 두 층위에 각각 적용된다 — ① 상위 이슈 간 정렬, ② 각 그룹 내부의 하위 이슈 간 정렬. 하위가 그룹을 이탈해 다른 부모의 하위와 섞이는 일은 없어야 한다.
- **근거**: 상위 간 정렬은 전체 데이터(모든 페이지)를 대상으로 해야 의미가 있으므로 클라이언트 정렬로는 불가능하다. 클라이언트가 페이지 내에서만 재정렬하면 정렬 의미가 왜곡된다.
- **영향**: AG Grid의 내장 컬럼 정렬을 비활성화한다 (9.3). 정렬 UI는 외부 컨트롤 또는 커스텀 헤더로 구현한다 (8.4).

### D6. 필터·페이지 상태의 단일 원본 = URL

- **결정**: 검색어, 필터, 정렬, 페이지는 모두 URL searchParams에 저장하고, 이를 유일한 원본으로 삼는다.
- **근거**: 공유, 뒤로가기, 새로고침 복원이 별도 구현 없이 해결된다. React Query의 queryKey와도 자연스럽게 연결된다.
- **영향**: URL 파싱/정규화 훅이 상태 계층의 진입점이 된다 (9.1).

### D7. 그리드의 역할 = 순수 렌더링

- **결정**: 정렬·필터·페이징을 모두 서버가 담당하므로, AG Grid는 **Client-Side Row Model에 "현재 페이지 데이터만" 주입해 표시하는 역할**로 한정한다. AG Grid 내장 pagination, 내장 sort, 내장 filter는 사용하지 않는다. 페이저는 그리드 외부의 커스텀 컴포넌트다.
- **근거**: AG Grid의 서버 연동용 row model(SSRM)은 Enterprise 의존에 자체 datasource 개념을 갖고 있어 React Query 캐시와 데이터 흐름이 이원화된다. Infinite Row Model은 무한 스크롤 UX라 페이지 요건과 맞지 않는다. "React Query가 페이지 데이터를 가져오고, 그리드는 받은 것을 그대로 그린다"가 이 조합에서 가장 단순하고 예측 가능하다. 상세 비교는 13장.
- **영향**: 그리드에 남는 책임은 컬럼 렌더링, 계층 표시(경로 A) 또는 들여쓰기(경로 B), 행 스타일뿐이다.

### D8. 렌더링 전략 = 라이선스에 따른 경로 A/B

- **결정**: Enterprise 보유 시 경로 A(Tree Data), 미보유 시 경로 B(flatten). 3장 참조.
- **영향**: 9.4 / 9.5 중 하나만 구현한다. API·서버·상태 관리 코드는 경로 전환 시에도 재사용된다.

## 5. API 설계

### 5.1 엔드포인트

단일 엔드포인트: `GET /api/issues`

계층 조회, 검색, 필터, 페이징을 모두 이 엔드포인트의 쿼리 파라미터로 처리한다. 별도의 하위 이슈 전용 엔드포인트는 MVP에서 만들지 않는다 (하위 lazy load 도입 시 추가 — 13장).

### 5.2 요청 파라미터

| 파라미터   | 타입   | 필수   | 기본값           | 설명                                                                                       |
| ---------- | ------ | ------ | ---------------- | ------------------------------------------------------------------------------------------ |
| page       | number | 아니오 | 1                | 1-base 페이지 번호. **상위 이슈 기준** (D1)                                                |
| size       | number | 아니오 | 20               | 페이지당 상위 이슈 수. 최대 100, 초과 시 100으로 절삭                                      |
| q          | string | 아니오 | —                | 키워드. 제목(title)과 키(key) 대상 부분 일치. 서버에서 trim 후 빈 문자열이면 무시          |
| status     | string | 아니오 | —                | 상태 코드 콤마 구분 다중값 (예: `IN_PROGRESS,REVIEW`). 코드 목록은 도메인 상태 정의를 따름 |
| assigneeId | string | 아니오 | —                | 담당자 ID 단일값                                                                           |
| sort       | string | 아니오 | `createdAt,desc` | `필드,방향` 형식. 방향은 `asc`/`desc`                                                      |

**sort 필드 화이트리스트**: `createdAt`, `updatedAt`, `key`, `title`, `status`. 화이트리스트 외 필드 또는 형식 오류는 **400 응답** (SQL 인젝션 및 인덱스 외 정렬 방지). 동일 정렬 기준이 상위 간·그룹 내 양쪽에 적용된다 (D5).

**매칭성 필터의 정의**: `q`, `status`, `assigneeId`가 이에 해당한다. D2의 "본인 OR 하위 매칭" 규칙과 D4의 matched 계산은 이 세 파라미터의 AND 결합을 술어로 사용한다. 세 개 모두 비어 있으면 필터 없음 상태이며 전 행 matched=true.

### 5.3 응답 구조

응답은 **중첩 구조**다. 상위 이슈 배열이 있고, 각 상위가 자신의 하위 배열을 `children` 필드로 품는다.

최상위 필드:

| 필드    | 타입   | 설명                                  |
| ------- | ------ | ------------------------------------- |
| content | array  | 상위 이슈 목록. 상위 간 정렬 순서대로 |
| page    | object | 페이징 메타                           |

content[] 항목 (상위 이슈):

| 필드         | 타입           | 설명                                                                     |
| ------------ | -------------- | ------------------------------------------------------------------------ |
| id           | string         | 행 고유 ID. 그리드 row ID로 사용되므로 상·하위 전체에서 유일해야 함      |
| key          | string         | 이슈 키 (예: REQ-42)                                                     |
| title        | string         | 제목                                                                     |
| status       | string         | 상태 코드                                                                |
| assigneeId   | string \| null | 담당자 ID                                                                |
| assigneeName | string \| null | 담당자 표시명                                                            |
| createdAt    | string         | ISO 8601                                                                 |
| updatedAt    | string         | ISO 8601                                                                 |
| matched      | boolean        | 직접 매칭 여부 (D4)                                                      |
| childCount   | number         | 하위 이슈 수. children 배열 길이와 동일하지만 표시 편의를 위해 명시 제공 |
| children     | array          | 하위 이슈 배열. 그룹 내 정렬 순서대로. 없으면 빈 배열 (null 금지)        |

children[] 항목 (하위 이슈): 상위와 동일한 필드 구성에서 `children`, `childCount`를 제외한다. 추가로 `parentId` (string, 소속 상위의 id)를 포함한다.

page 객체:

| 필드         | 타입   | 설명                                       |
| ------------ | ------ | ------------------------------------------ |
| number       | number | 현재 페이지 (1-base)                       |
| size         | number | 적용된 페이지 크기                         |
| totalParents | number | 필터 조건을 만족하는 **상위 이슈 총 건수** |
| totalPages   | number | ceil(totalParents / size)                  |

**중첩 구조를 채택한 이유**: 그룹 무결성이 응답 형태 자체로 보장되고, AG Grid Enterprise의 `treeDataChildrenField`(Nested Records)에 변환 없이 직결된다. 대안으로 flat 배열 + `parentId` 계약도 가능하며 AG Grid의 `treeDataParentIdField`(Self-Referential Records)가 이를 지원하지만, 클라이언트에서 그룹 무결성을 다시 검증해야 하므로 본 설계는 중첩을 채택한다. 향후 부분 행 업데이트(transaction) 요구가 생기면 flat 계약으로의 전환을 검토한다 (9.4의 제약 참고).

### 5.4 경계·오류 규칙

- **page 범위 초과**: 서버는 요청대로 처리하고 빈 `content`를 반환한다 (오류 아님). 프론트는 응답의 `totalPages`를 보고 `page > totalPages && totalPages > 0`이면 마지막 유효 페이지로 보정 재요청, `totalPages === 0`이면 빈 상태를 표시한다.
- **LIKE 특수문자**: 서버는 `q`의 `%`, `_`, 이스케이프 문자를 escape 처리한다 (Oracle LIKE의 ESCAPE 절 사용). 클라이언트는 원문 그대로 전송한다.
- **오류 응답**: 400(파라미터 형식/화이트리스트 위반), 500(서버 오류). 오류 본문은 조직 표준 오류 포맷을 따른다.
- **인증/권한**: 기존 시스템의 공통 인증 체계를 따르며 본 문서 범위 외.

## 6. 서버 구현 지침 — 2단계 쿼리 패턴

이 화면의 전형적 실패 패턴은 "한 방 쿼리 + LIMIT"으로 해결하려다 LIMIT이 하위 행까지 잡아먹어 페이징이 꼬이는 것이다. 반드시 두 단계로 분리한다.

### 6.1 1단계: 페이징된 상위 ID 선별

- 대상: `parent_id IS NULL`인 행만.
- 필터: (본인이 매칭 술어 만족) OR (EXISTS: 자신을 parent로 갖는 하위 중 매칭 술어 만족 행 존재). 매칭 술어는 5.2의 q/status/assigneeId AND 결합.
- 정렬: sort 파라미터의 필드·방향. 동률 시 안정성을 위해 id를 2차 정렬 키로 추가.
- 페이징: 정렬 후 OFFSET/FETCH (Oracle 12c+ 문법)로 상위 이슈 size건의 **ID 목록만** 선별.
- count: 동일 WHERE로 상위 이슈만 카운트하여 totalParents 산출. 목록 쿼리와 count 쿼리의 WHERE가 항상 같도록 술어를 한 곳에서 조립한다.

### 6.2 2단계: 상위 + 하위 일괄 로드 및 조립

- 1단계에서 얻은 ID 목록으로 `id IN (목록) OR parent_id IN (목록)` 단일 쿼리로 상위·하위 전 행을 한 번에 로드한다. **부모별 개별 쿼리(N+1)는 금지.**
- 애플리케이션 레이어에서 parentId 기준으로 그룹핑하여 중첩 구조를 조립한다. 상위 순서는 1단계 ID 목록 순서를 유지하고, 각 그룹의 children은 동일 정렬 기준으로 정렬한다 (2단계 쿼리에 ORDER BY를 넣어도 되고 앱에서 정렬해도 됨 — 페이지 규모라 어느 쪽이든 무방).

### 6.3 matched 계산

- 2단계 로드 후 애플리케이션에서 각 행(상위·하위)에 매칭 술어를 재평가하여 matched를 부여한다. 페이지 규모(상위 최대 100 + 하위)이므로 앱 레이어 평가로 충분하다.
- 매칭성 필터가 모두 비어 있으면 전 행 true (D4).
- 주의: 1단계 술어(포함 여부)와 matched 술어(표시 구분)는 동일한 조건 정의를 공유해야 한다. 별도 구현으로 두 곳이 어긋나면 "포함됐는데 matched인 행이 하나도 없는" 모순이 생긴다.

### 6.4 인덱스 및 확장 고려

- 인덱스: `parent_id`, `(parent_id, createdAt)`, status·assigneeId 등 필터 컬럼. 정렬 화이트리스트 필드는 인덱스 가능 여부를 함께 검토.
- `LIKE '%…%'` 중간 일치는 인덱스를 타지 못한다. 내부 툴 데이터 규모에서는 당분간 허용하되, 검색 지연이 체감되면 Oracle Text 기반 전문 검색으로 전환을 검토한다.
- 두 쿼리 사이 데이터 변경 가능성: 단일 읽기 트랜잭션으로 묶는 것을 권장하되, 내부 툴 특성상 찰나의 불일치(그 사이 하위 추가 등)는 허용 가능한 수준으로 판단한다.

## 7. 화면 설계

### 7.1 레이아웃

위에서 아래로 3개 영역:

1. **필터 바**: 검색 인풋(placeholder에 검색 대상 명시: "키 또는 제목 검색"), 상태 멀티셀렉트, 담당자 셀렉트, 정렬 셀렉트(MVP — 8.4 참고), 필터 초기화 버튼.
2. **그리드 본문**: 계층 그리드. 높이는 뷰포트 기반 고정 높이 + 내부 스크롤 (페이지당 행 수가 가변이므로 — D1).
3. **하단 바**: 좌측 "상위 이슈 {totalParents}건" 카운트, 우측 페이저. 카운트에 단위(상위 이슈)를 반드시 명시한다 — 화면의 행 수와 페이저 숫자가 다른 이유를 사용자에게 설명하는 장치다.

### 7.2 컬럼 구성

| 컬럼             | 내용                                                        | 비고                                                                                 |
| ---------------- | ----------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| 이슈 (그룹 컬럼) | 키 + 제목 조합 표시. 계층 표현(펼침 아이콘/들여쓰기)을 담당 | 경로 A: autoGroupColumnDef, 경로 B: 커스텀 셀 렌더러. 너비 가장 넓게, 좌측 고정 권장 |
| 상태             | 상태 뱃지                                                   |                                                                                      |
| 담당자           | assigneeName                                                | 미지정 시 "—"                                                                        |
| 하위             | childCount                                                  | 상위 행에만 표시. 0이면 공백                                                         |
| 생성일           | createdAt 포맷팅                                            | 기본 정렬 기준                                                                       |
| 수정일           | updatedAt 포맷팅                                            |                                                                                      |

### 7.3 행 시각 규칙

- 하위 행: 그룹 컬럼에서 1단계 들여쓰기 (경로 A는 그리드 제공, 경로 B는 커스텀).
- 컨텍스트 상위 (matched=false인 상위): 텍스트 dimmed 처리 + 그룹 컬럼 옆 "하위 매칭" 뱃지. "이 행 자체는 검색에 걸린 게 아니다"를 전달.
- 매칭 안 된 하위 (matched=false인 하위): dimmed 처리만. 뱃지 없음.
- 하위 0건 상위: 펼침 아이콘 없이 일반 행처럼 표시 (leaf 취급).
- 필터 없음 상태: 전 행 matched=true이므로 dimmed/뱃지 없음.

### 7.4 인터랙션 명세

| 이벤트                         | 동작                                               |
| ------------------------------ | -------------------------------------------------- |
| 검색어 입력                    | 300ms debounce 후 URL의 q 갱신 + page=1 리셋       |
| 상태/담당자 필터 변경          | 즉시 URL 갱신 + page=1 리셋                        |
| 정렬 변경                      | URL의 sort 갱신 + page=1 리셋                      |
| 페이지 이동                    | URL의 page 갱신, 응답 반영 후 그리드 스크롤 최상단 |
| 필터 초기화                    | q/status/assigneeId 제거, sort는 기본값, page=1    |
| 새로고침 / URL 공유 / 뒤로가기 | URL 파싱만으로 동일 화면 재현 (D6)                 |
| 행 클릭                        | 상세 화면 이동 훅만 마련 (상세는 범위 외)          |

**정렬 UI에 대한 결정**: MVP는 필터 바의 정렬 셀렉트(외부 컨트롤)로 구현한다. 컬럼 헤더 클릭 정렬 UX를 원하면, 그리드 내장 정렬을 켜는 것이 아니라 **커스텀 헤더 컴포넌트가 클릭을 받아 URL의 sort를 갱신**하는 방식으로 확장한다. 그리드 내장 정렬을 켜면 클라이언트가 현재 페이지 행을 재정렬해 서버 정렬 결과와 충돌하고, 경로 B에서는 flat 행이 뒤섞여 그룹이 붕괴하므로 어떤 경우에도 켜지 않는다 (D5, D7).

### 7.5 로딩·빈·에러 상태

- 최초 로딩: 그리드 로딩 오버레이 또는 스켈레톤.
- 페이지/필터 전환: 이전 데이터를 유지한 채 로딩 (9.2의 placeholderData). 전환 중에는 페이저 버튼 비활성화로 연타 방지.
- 결과 0건: "조건에 맞는 이슈가 없습니다" 오버레이 + 필터 초기화 버튼 노출.
- 에러: 오류 메시지 + 재시도 버튼. 그리드 영역을 대체.

## 8. 프론트 구현 지침

### 8.1 상태 관리 (URL ↔ 쿼리)

- URL searchParams를 파싱·정규화하는 훅을 만든다. 산출물은 5.2 파라미터와 1:1 대응하는 정규화 객체 (기본값 채움, size 절삭, sort 화이트리스트 검증 포함).
- 이 정규화 객체가 그대로 React Query queryKey의 구성 요소가 된다: `['issues', 정규화객체]`. 객체 필드 순서·형태를 항상 동일하게 유지해 캐시 키 안정성을 보장한다.
- 검색 인풋은 로컬 상태로 즉시 반영하고, debounce 후에만 URL에 쓴다 (URL이 원본, 인풋은 draft).
- URL 갱신은 replace 방식(히스토리 오염 방지)을 기본으로 하되, 페이지 이동만 push로 남길지는 팀 관례에 따른다.

### 8.2 TanStack Query v5 규칙

- v5 기준으로 작성한다. v4의 `keepPreviousData: true` 옵션은 v5에서 제거되었고, `placeholderData`에 라이브러리가 제공하는 `keepPreviousData` 헬퍼를 전달하는 방식으로 대체되었다. 페이지·필터 전환 시 깜빡임 방지를 위해 이를 적용한다.
- `isPlaceholderData` 상태로 "이전 데이터 표시 중"을 감지해 페이저 비활성화(7.5)에 사용한다.
- staleTime은 30초 수준 (내부 툴, 이슈 상태가 자주 변함). 창 포커스 시 재조회는 팀 선호에 따라. 실패 재시도는 1회.
- page 보정 로직 (5.4): 응답 수신 후 totalPages와 현재 page를 비교해 초과 시 URL을 마지막 유효 페이지로 교정한다.

### 8.3 AG Grid 공통 설정 (경로 A·B 공통, v33+ 기준)

- **모듈 등록 필수**: v33부터 모든 기능이 모듈화되어, 그리드 생성 전 모듈 등록이 없으면 동작하지 않는다. 번들 크기를 신경 쓰지 않는 단계에서는 `AllCommunityModule`(경로 B) 또는 `AllEnterpriseModule`(경로 A) 번들을 전역 등록(ModuleRegistry)한다. React에서는 v35.1부터 AgGridProvider 컴포넌트로 모듈을 공급하는 방식도 공식 지원된다. 최적화가 필요해지면 공식 Module Selector로 개별 모듈을 추린다.
- **개발 검증**: 개발 빌드에서 유효성 경고 모듈(ValidationModule 또는 enableDevValidations)을 활성화하면 설정 오류를 콘솔 경고로 잡아준다. 프로덕션 번들에서는 제외한다. 번들에의 기본 포함 여부가 버전에 따라 다르므로 사용 버전의 공식 문서를 확인한다.
- **테마**: v33에서 도입된 Theming API(테마 객체를 그리드 옵션으로 전달)를 사용한다. CSS 파일 import 기반 v32 방식은 공식 문서에서 Legacy Themes로 분류되어 있으므로 신규 코드에 사용하지 않는다.
- **행 식별**: `getRowId`를 응답의 `id`로 지정한다. 페이지 전환 시 행 재사용·애니메이션이 안정된다.
- **내장 기능 비활성**: pagination 미설정, 전 컬럼 `sortable: false`, 그리드 필터 미사용 (D5, D7). 데이터는 페이지 응답 단위로 rowData 전체 교체.

### 8.4 경로 A: Enterprise Tree Data

- `treeData: true` + `treeDataChildrenField: 'children'`을 설정하면 5.3의 중첩 응답을 **변환 없이 rowData로 직접 주입**할 수 있다 (공식 Nested Records 방식).
- **함정 1 — 그룹 표시값**: Nested Records 방식에서 그리드는 기본적으로 row ID를 그룹 표시값으로 사용한다. 반드시 `autoGroupColumnDef`의 field를 지정(예: title)하거나, innerRenderer로 키+제목 조합을 커스텀한다 (7.2의 그룹 컬럼).
- **함정 2 — transaction 미지원**: 공식 문서상 `treeDataChildrenField` 사용 시 transaction update(부분 행 갱신)를 지원하지 않는다. 본 설계는 페이지 단위 전체 교체(D7)이므로 영향 없지만, 향후 실시간 부분 갱신 요구가 생기면 flat + `treeDataParentIdField`(Self-Referential, 공식 문서상 가장 성능 좋은 공급 방식이며 transaction 지원) 계약으로 전환을 검토한다.
- 전체 펼침 기본: `groupDefaultExpanded: -1`. 검색 시 매칭된 하위가 접혀서 안 보이는 상황을 방지한다.
- 그룹 행 카운트 표시 등 부가 장식은 autoGroupColumnDef의 cellRendererParams로 조정한다. childCount 컬럼(7.2)과 중복되지 않게 그룹 셀 내 카운트는 끈다.
- matched 시각화: rowClassRules로 matched=false 행에 dimmed 클래스를 부여하고, 뱃지는 그룹 컬럼 innerRenderer에서 조건 렌더링한다.

### 8.5 경로 B: Community flatten

- 응답 수신 후 클라이언트에서 flatten한다: 각 상위 뒤에 그 하위들을 순서대로 붙이고, 각 행에 `depth`(0/1)와 `isChild` 파생 필드를 부여한다. 이 flat 배열이 rowData다. 서버가 이미 두 층위 정렬을 마쳤으므로 flatten은 순서 보존만 하면 된다.
- 계층 표현: 그룹 컬럼(7.2)을 커스텀 셀 렌더러로 구현 — depth 기반 들여쓰기 + 하위 표시 아이콘(└ 등) + 키·제목.
- 접기/펼치기: **MVP는 항상 펼침 고정을 권장** (상태 관리 불필요, 검색 결과 가시성 보장). 꼭 필요하면 접힌 상위 ID Set을 React 상태로 두고 flatten 시 해당 하위를 제외하는 방식으로 구현한다 — 페이지 규모 데이터라 재계산 비용은 무시 가능하다.
- 그룹 시각 구분: rowClassRules로 상위/하위 배경 톤 차이, matched=false dimmed, 컨텍스트 상위 뱃지는 셀 렌더러에서.
- 이 경로의 전제: 정렬·필터·페이징이 전부 서버에 있으므로(D5, D7) 그리드가 flat 배열의 순서를 건드릴 일이 없다. 이 전제가 깨지는 설정(내장 sort 활성 등)을 절대 넣지 않는다.

### 8.6 페이저 컴포넌트

- 그리드 외부 커스텀 컴포넌트. totalPages 기반 페이지 번호 + 이전/다음. 현재 페이지, 총 페이지, "상위 이슈 N건" 표기.
- isPlaceholderData 동안 비활성화 (7.5).

### 8.7 컴포넌트·파일 구조 제안

기존 팀 컨벤션(페이지 하위 `_components/` + 동일 위치 `*.hooks.ts` / `*.utils.ts`)을 따른다:

- 페이지: 필터 바 + 그리드 + 하단 바 조립만.
- hooks: URL 파라미터 정규화 훅, 이슈 목록 쿼리 훅 (queryKey·placeholderData·page 보정 포함).
- utils: flatten 함수(경로 B), 컬럼 정의, 날짜 포맷터.
- 도메인 컴포넌트가 useQuery를 직접 소유하고, 공유가 필요하면 동일 queryKey로 해결한다 (props drilling 금지 — 기존 컨벤션).

## 9. 엣지 케이스

| 케이스                         | 처리                                                                                                          |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------- |
| 하위만 매칭                    | 상위를 컨텍스트로 포함 (D2), matched=false + 뱃지 (7.3)                                                       |
| 하위 0건 상위                  | leaf 취급, 펼침 아이콘 없음. children은 빈 배열                                                               |
| page 범위 초과                 | 서버는 빈 content, 프론트가 마지막 유효 페이지로 보정 (5.4)                                                   |
| size 상한 초과                 | 서버가 100으로 절삭 (5.2)                                                                                     |
| 검색어에 %, _ 포함             | 서버 escape (5.4). "50%"를 검색하면 리터럴로 동작해야 함                                                      |
| 고아 하위 (상위 삭제됨)        | 데이터 무결성(FK)으로 발생하지 않는 것이 전제. 상위 판별은 parent_id IS NULL로만                              |
| 결과 0건                       | 빈 상태 오버레이 + 필터 초기화 유도 (7.5)                                                                     |
| 요청 경합 (연속 필터 변경)     | React Query가 최신 queryKey 기준으로 처리. 별도 구현 불필요                                                   |
| fan-out 큰 상위 (하위 수십 건) | 페이지 높이 증가 → 그리드 내부 스크롤로 흡수 (7.1). 평균 fan-out이 지속적으로 크면 하위 lazy load 검토 (13장) |
| 상·하위 ID 충돌                | id는 전체 이슈에서 유일 (5.3). 동일 테이블 PK라면 자연 보장                                                   |

## 10. 검증 체크리스트 (Acceptance Criteria)

- [ ] 어떤 페이지에서도 하위 이슈가 자신의 상위와 분리되어 표시되지 않는다 (그룹 무결성).
- [ ] 검색어가 하위에만 걸려도 해당 상위+그룹 전체가 표시되고, 상위에 "하위 매칭" 뱃지가 보인다.
- [ ] matched=false 행이 시각적으로 구분된다 (dimmed).
- [ ] 정렬 변경이 현재 페이지가 아닌 전체 데이터 기준으로 동작한다 (1페이지 마지막 행보다 2페이지 첫 행이 정렬상 뒤).
- [ ] 그룹 내 하위들도 동일 정렬 기준으로 정렬되어 있고, 다른 그룹과 섞이지 않는다.
- [ ] 하단 카운트가 "상위 이슈 N건"으로 표기되고 totalParents와 일치한다.
- [ ] URL을 복사해 새 탭에서 열면 동일한 필터·정렬·페이지 상태가 재현된다.
- [ ] 페이지 전환 시 이전 데이터가 유지되며 깜빡임이 없고, 전환 중 페이저가 비활성화된다.
- [ ] 필터 변경 시 page가 1로 리셋된다.
- [ ] totalPages를 초과하는 page로 진입하면 마지막 유효 페이지로 자동 보정된다.
- [ ] "%"가 포함된 검색어가 리터럴로 검색된다.
- [ ] 결과 0건 시 빈 상태와 필터 초기화 버튼이 보인다.
- [ ] (경로 A) Enterprise 라이선스 키가 적용되어 있고 콘솔에 워터마크/경고가 없다.

## 11. 구현 순서 제안

1. **P0 확인**: AG Grid 라이선스 → 경로 A/B 확정 (3장).
2. **API 계약 고정 + mock**: 5장 계약대로 MSW 핸들러 작성 (기존 mock-first 관행). 필터·페이징·matched 로직을 mock에도 구현해 프론트를 서버 없이 완성 가능하게 한다.
3. **프론트 뼈대**: URL 정규화 훅 → 쿼리 훅 → 그리드 렌더(경로별) → 페이저 → 필터 바 순.
4. **서버**: 1단계 쿼리 + count → 2단계 로드·조립 → matched 계산 → 5.4 경계 규칙.
5. **연동 및 스타일**: matched 시각화, 로딩/빈/에러 상태.
6. **검증**: 10장 체크리스트 전 항목. 특히 그룹 무결성과 전역 정렬은 2페이지 이상 데이터로 확인한다.

## 12. 채택하지 않은 대안 (기록)

| 대안                                              | 기각 사유                                                                                                                                                            |
| ------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 행 단위 flatten 페이징                            | 하위가 페이지 경계에서 잘려 그룹 무결성 붕괴 (D1 근거)                                                                                                               |
| AG Grid Server-Side Row Model                     | Enterprise 의존 + 자체 datasource로 React Query 캐시와 데이터 흐름 이원화 + 이 데이터 규모에 과한 복잡도. 계층이 깊고 노드 lazy load가 필수인 대규모 트리에서 재검토 |
| AG Grid Infinite Row Model                        | 무한 스크롤 기반이라 페이지 번호 요건과 불일치, 그룹 무결성 관리도 어려움                                                                                            |
| AG Grid 내장 pagination                           | 그리드가 가진 데이터(현재 페이지) 안에서의 페이징이라 서버 페이징과 개념 불일치. 외부 페이저로 대체 (D7)                                                             |
| 하위 매칭 시 하위를 최상위로 승격 (Linear 스타일) | 그룹핑 요건(요구사항 2)과 충돌 (D2 근거)                                                                                                                             |
| 매칭된 하위만 반환 + 나머지 lazy load             | MVP 복잡도 대비 이득 부족. 평균 fan-out이 커지면(예: 20+) 하위 전용 엔드포인트 추가로 확장 (D3 근거)                                                                 |
| 검색어 하이라이트                                 | MVP 범위 외. 도입 시 프론트에서 q 기준 마크업 (서버는 관여 안 함)                                                                                                    |

## 13. 참고 문서 (공식)

- AG Grid — Tree Data Overview: https://www.ag-grid.com/react-data-grid/tree-data/
- AG Grid — Tree Data: Supplying Data (Data Paths / Nested / Self-Referential 비교): https://www.ag-grid.com/react-data-grid/tree-data-data/
- AG Grid — Tree Data: Nested Records (`treeDataChildrenField`): https://www.ag-grid.com/react-data-grid/tree-data-nesting/
- AG Grid — Tree Data: Self-Referential Records (`treeDataParentIdField`): https://www.ag-grid.com/react-data-grid/tree-data-self-referential/
- AG Grid — Modules (모듈 등록, AllCommunityModule/AllEnterpriseModule, Module Selector): https://www.ag-grid.com/react-data-grid/modules/
- AG Grid — Theming (v33+ Theming API): https://www.ag-grid.com/react-data-grid/theming/
- AG Grid — What's New / 버전 확인: https://www.ag-grid.com/whats-new/
- TanStack Query v5 — Paginated Queries (placeholderData + keepPreviousData 헬퍼): https://tanstack.com/query/v5/docs/framework/react/guides/paginated-queries
- TanStack Query — v5 Migration Guide: https://tanstack.com/query/v5/docs/framework/react/guides/migrating-to-v5
